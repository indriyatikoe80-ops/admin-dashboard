name: Keep Hugging Face Awake

on:
  schedule:
    # Berjalan setiap 10 menit
    - cron: '*/10 * * * *'
  workflow_dispatch: # Memungkinkan Anda menjalankan secara manual untuk tes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: |
          curl -s https://candrabuwana80-api-belajar-anak.hf.space/health
